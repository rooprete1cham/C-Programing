#include <stdio.h>

int main()
{

    int x = 1;        // 변수 x에 1 저장

    int y = 2;        // 변수 y에 2 저장

    int z = x + y;    // x와 y의 합을 z에 저장

    printf("x: %d\n", x);
    printf("y: %d\n", y);
    printf("z: %d\n", z);

    return 0;
}
